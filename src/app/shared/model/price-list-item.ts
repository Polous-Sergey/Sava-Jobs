export class PriceListItem {
    constructor(title, subTitle, price) {
        this.title = title;
        this.subTitle = subTitle;
        this.price = price;
    }

    title: string;
    subTitle: string;
    price: string;
}
